# asadbinali.com

The site and its backend. See the [README at the repository root](../README.md) for what's here, how to run it locally, and how it deploys.

```bash
npm run setup   # install the site and the backend
npm run dev     # site on http://localhost:3000, backend on http://localhost:5000
npm run build   # production build into build/
```
